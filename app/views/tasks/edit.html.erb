<h1>id: <%= @task.id %>タスク編集ページ</h1>
        <%= form_with(model: @task)do |f| %>
        <%= f.label :content %>
        <%= f.text_field :content %>
        
        <%= f.submit 'タスク' %>
    <% end %>
    
        <%= link_to '一覧戻る',task_path %>
    